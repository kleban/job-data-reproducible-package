# Python Paper-Analytics Outputs

This directory stores outputs generated by the Python paper-analytics notebooks.

| Directory | Producer | Contents | Current status |
|---|---|---|---|
| `figures/` | Notebook 02 | Vacancy, occupation, skill, remote-work, and conflict-related figures | Not generated in the public workspace |

The final R manuscript tables and figures are written inside the self-contained R project under:

```text
code/paper-analytics/reproducibility_package/output/
```

Do not mix the two output trees until every Python figure has been checked against the manuscript and the R output map. Only outputs corresponding to the paper or documented supplementary checks should be retained in the final release.
