[
  {
    "name": "selectRegion",
    "description": "Extract structured location information from a region title. Handles remote jobs, attempts to infer country based on language when only 'remote' is specified.",
    "parameters": {
      "type": "object",
      "properties": {
        "original": {
          "type": "string",
          "description": "Original input string describing the location (e.g., 'Berlin, Germany' or 'віддалено')."
        },
        "city": {
          "type": "string",
          "description": "City name in English if available, or 'remote' if the job is remote."
        },
        "district": {
          "type": "string",
          "description": "District name in English if available, or 'remote' if the job is remote."
        },
        "region": {
          "type": "string",
          "description": "Administrative region in English if available, or 'remote' if the job is remote. Select Oblast not Rayon"
        },
        "country": {
          "type": "string",
          "description": "Country name in English. If the job is remote-only and the input includes a language-specific remote keyword, the country is inferred from the language."
        },
        "latitude": {
          "type": "string",
          "description": "Latitude as a decimal string, or an empty string if unknown or remote."
        },
        "longitude": {
          "type": "string",
          "description": "Longitude as a decimal string, or an empty string if unknown or remote."
        },
        "is_remote": {
          "type": "string",
          "enum": ["true", "false"],
          "description": "'true' if the job is remote (e.g., 'Remote', 'віддалено'), otherwise 'false'."
        }
      },
      "required": [
        "original",
        "city",
        "region",
        "country",
        "latitude",
        "longitude",
        "is_remote"
      ]
    }
  }
]
