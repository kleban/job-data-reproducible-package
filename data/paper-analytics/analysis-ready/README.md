# Analysis-ready data

The final vacancy-level Parquet file generated by `notebooks/paper-analytics/01_build_analysis_ready_vacancy_data.ipynb` is stored here:

- `vacancies_2021_2025_collapsed_by_id.parquet`

Subsequent Python notebooks and the R project should read this file rather than repeating the Stage 5 aggregation.
